import React, { Component } from 'react';
import Subtitle from './Subtitle';

class Layers extends Component {
  render() {
    return (
      <div>
        <Subtitle text={'Layers'} />
        Layers
      </div>
    );
  }
}

export default Layers;
